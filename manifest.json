{
  "manifest_version": 3,
  "name": "Cookie Dumper",
  "version": "2.1.0",
  "description": "Secure, on-demand cookie dumping: per-site .env served over a token-gated localhost endpoint by a local native host. Nothing written to disk.",
  "permissions": [
    "cookies",
    "tabs",
    "activeTab",
    "alarms",
    "storage",
    "nativeMessaging"
  ],
  "host_permissions": [
    "<all_urls>"
  ],
  "background": {
    "service_worker": "background.js"
  },
  "action": {
    "default_title": "Cookie Dumper",
    "default_popup": "popup.html"
  }
}
