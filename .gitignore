# Dumped cookie output — contains live session tokens, never commit
.env
*.env
!*.env.example

# Extension signing key — private, never commit (the public half lives in manifest.json "key")
key.pem
*.pem

# Build output / release artifacts
dist/
*.zip
*.crx

# OS / editor cruft
.DS_Store
*.swp
.idea/
.vscode/

# generated by `cookiedumper host install` (absolute-interpreter launcher)
host-launcher.sh
